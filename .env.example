# 🤖 Groq API Configuration Key
# Replace the mock value with your actual Groq API Key (from https://console.groq.com)
# If left blank, the application will fallback to the local matching engine (TF-IDF + Cosine Similarity).

GROQ_API_KEY=gsk_your_key_here
